# Project properties
project.name = NativeGATracker
version.num = 2.0.4
project.name.versioned=${project.name}-v${version.num}

extension.descriptor = ${basedir}/build/extension.${platform}.xml
ios.platform.options = ${basedir}/build/platform-iphone.xml

# Certificate
crt.file = ${basedir}/build/certificate.p12
crt.password = password

# Folders
tmp.dir = ${basedir}/temp
bin.dir = ${basedir}/bin
lib.dir = ${basedir}/libs
ext.dir = ${basedir}/source
doc.dir = ${basedir}/docs
dist.dir = ${basedir}/release

bin.def.dir = ${bin.dir}/native/default
bin.and.dir = ${bin.dir}/native/android
bin.ios.dir = ${bin.dir}/native/ios
bin.iossim.dir = ${bin.dir}/native/sim

src.def.dir = ${ext.dir}/actionscript/default
src.as3.dir = ${ext.dir}/actionscript/agnostic
src.and.dir = ${ext.dir}/android
src.ios.dir = ${ext.dir}/ios